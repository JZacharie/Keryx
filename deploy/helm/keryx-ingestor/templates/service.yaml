apiVersion: v1
kind: Service
metadata:
  name: {{ include "keryx-orchestrator.fullname" . }}
  labels:
    {{- include "keryx-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keryx-orchestrator.selectorLabels" . | nindent 4 }}
