apiVersion: v1
kind: Service
metadata:
  name: {{ include "keryx-ingestor.fullname" . }}
  labels:
    {{- include "keryx-ingestor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keryx-ingestor.selectorLabels" . | nindent 4 }}
